<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vssquare.hardoinews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SplashScreen"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PermissionTransferToHomeActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.DEFAULT"/>
        </intent-filter>
        </activity>
        <activity android:name=".Contact_Us"
            android:configChanges="screenSize|orientation"
            android:label="@string/contact"/>
        <activity android:name=".Health"
            android:configChanges="screenSize|orientation"
            android:label="@string/health"/>
        <activity android:name=".Games"
            android:configChanges="screenSize|orientation"
            android:label="@string/games"/>
        <activity android:name=".Crime"
            android:configChanges="screenSize|orientation"
            android:label="@string/crime"/>
        <activity android:name=".Education"
            android:configChanges="screenSize|orientation"
            android:label="@string/education"/>
        <activity android:name=".Politics"
            android:configChanges="screenSize|orientation"
            android:label="@string/politics"/>
        <activity android:name=".Uttar_pradesh"
            android:configChanges="screenSize|orientation"
            android:label="@string/UP"/>
        <activity android:name=".Desh"
            android:configChanges="screenSize|orientation"
            android:label="@string/desh"/>
        <activity android:name=".WPPostDetails"
            android:configChanges="screenSize|orientation"
            android:label="@string/main_title"/>
        <activity android:name=".MainActivity"
            android:configChanges="screenSize|orientation"
            android:theme="@style/MainActivityTheme"
            />
    </application>

</manifest>